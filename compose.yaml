services:
  app:
    image: node:20
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - '3000:3000'
    env_file:
      - .env.development
    command: sh -c "npm install && npm run dev"
    tty: true
